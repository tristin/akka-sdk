include classpath("region1.conf")

kalix.proxy.deployment {
  regions = [
    {
      name = region1
      self = false
      primary = true
      ingress = "localhost:9000"
    },
    {
      name = region2
      self = true
      ingress = "localhost:9001"
    }
  ]
}

akka.persistence.r2dbc.connection-factory.port = 5433

kalix.proxy.replicated-entity.kve-replication-enabled=true
